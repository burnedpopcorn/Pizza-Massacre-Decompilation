function Scr_GameJolt_Parameters(argument0)
{
	// removed just in case
	/*
    var str, first, a;
    
    str = "";
    first = !string_pos("?", argument0);
    
    for (a = 1; a < argument_count; a += 2)
    {
        if (first)
            str = "?" + string(argument[a]) + "=" + argument[a + 1];
        else
            str += ("&" + string(argument[a]) + "=" + argument[a + 1]);
        
        first = false;
    }
    
    return argument0 + str;
	*/
}
